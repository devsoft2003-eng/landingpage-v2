Place the signed Android package here using the filename stored in MySQL (`products.file_path`).

Default expected file:

    kartavyanama.apk

Do not put APK files in /public. Downloads are streamed only after registration, CAPTCHA, and token checks.
